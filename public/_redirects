# Clean URL for the how-to guide (so /how-to-use-the-dor serves the page)
/how-to-use-the-dor   /how-to-use-the-dor.html   200

# Netlify Functions (must come before the SPA fallback below, or this file's
# own /* rule swallows /api/* first — netlify.toml's [[redirects]] never wins
# once this file exists, so the api rule has to live here, not just there)
/api/*  /.netlify/functions/:splat  200

# SPA fallback (must stay last)
/*  /index.html  200
